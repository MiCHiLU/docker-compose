version: "3"
services:

  travis-worker-bazel:
    build:
      context: dockerfile/travis-worker-bazel
    volumes:
    - $PWD:/home/travis/src:rw
    entrypoint:
    - /bin/bash
    - -c
    - |
      ./setup.sh && ./src/build.sh
